import { SearchExceptionsPipe } from './search-exceptions.pipe';

describe('SearchExceptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchExceptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
